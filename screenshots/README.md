# Screenshots Folder

Save the required submission screenshots here with these exact filenames:

- `dashboard.png` - cloud deployment dashboard
- `running.png` - service running / public URL visible
- `test.png` - successful API test result

Recommended sources:

- Railway or Render deployment dashboard
- Browser tab showing `/health` or the running service
- Terminal or Postman result showing a successful authenticated request
